#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    char ch;           // variable declaration
    printf("Enter a character : ");
    scanf("%c",&ch);  // user input
    printf("\nThe ascii value of the ch variable is : %d", ch);
   return 0;
}


/*write a c program to read a character from the user and display
  whether the character is a vowel or a consonent . Use IF ELSE
  statement , ELSE IF ladder , SWITCH case for the same (ALL 3)*/
