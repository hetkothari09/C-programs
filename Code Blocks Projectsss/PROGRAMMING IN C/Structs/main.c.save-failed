#include <stdio.h>
#include <conio.h>

struct Student {
char name [30];
char major [30];
int age ;
double gpa;
};

int main()
{
  struct Student student1;
  student1.age =22;
  student1.gpa = 3.4;
  strcpy(student1.name , "Het");
  strcpy(student1.major , "Science");

  struct Student student2;
  student2.age = 42;
  student2.gpa =3.1;
  strcpy(student2.name, "kothari");
  strcpy(student2.major, "Commerce");

  printf("%s",student1.name);

    return 0;
}
